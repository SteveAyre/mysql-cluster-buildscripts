First checkout the packaging for your intended target:
	$ scripts/checkout [TARGET]

To build packages:
	$ scripts/build [TARGET]

The default target is wheezy.
A squeeze target is also available, which is a squeeze-backport of the wheezy package.

