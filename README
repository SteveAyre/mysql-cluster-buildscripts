First checkout the packaging for your intended target:
	$ scripts/checkout [TARGET]

To build packages:
	$ scripts/build [TARGET]

The default target is your current distribution:
	$ scripts/checkout
	$ scripts/build

Branches:
	The master branch targets wheezy.
	The squeeze-backport branch targets squeeze.

